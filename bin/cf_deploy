#!/usr/bin/env ruby
# vim: ft=ruby

require "cf_deployer/cf_deploy"
require "cf_deployer/command_runner"

cf_deploy = CfDeploy.new(Cli.new(ARGV).parse!)

runner = CommandRunner.new

Bundler.with_clean_env do
  cf_deploy.deploy_to_aws(runner)
end
